/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'enter.online',
      'darwin.md',
      'accent.md',
      'maximum.md',
      'i.simpalsmedia.com'
    ]
  }
}

module.exports = nextConfig
